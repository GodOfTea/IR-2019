Чтобы не делать ллишинх файлов

Вот по этой строке запроса не очень понятно, как искать несколкьо слов из названия
Ведь тут только по одному слову ищется?

await dbProvider.GetRowsAsync(condition: $"name ~~* '%{query}%' {yearCondition}", count: 10);
